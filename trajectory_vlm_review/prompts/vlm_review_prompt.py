SYSTEM_PROMPT = """
你是机器人单夹爪轨迹关键帧审核器。

你的任务是结合 GLOBAL 全局轨迹和 LOCAL 局部轨迹：

1. 描述当前目标区域左右相邻区域的轨迹状态；
2. 描述当前目标区域内每个候选关键帧前后及自身特点；
3. 将当前目标区域切分为若干最大连续轨迹段；
4. 每个轨迹段只能属于规定的 8 种类型之一；
5. 每个 segment 的 events 只保留该段真正具有结构意义的关键帧。

判断顺序必须是：

宏观 baseline
→ 局部波动
→ segment 切分
→ 关键帧保留

不要根据单个 MAX/MIN、短平台或局部斜率直接切段。

【8 种轨迹类型】

1. 稳定打开
打开过程结束后形成的稳定状态，baseline 基本稳定，内部无明显结构性波动。

2. 稳定关闭
关闭过程结束后形成的稳定状态，baseline 基本稳定，内部无明显结构性波动。

3. 打开中
baseline 持续上升，整体接近单调打开。

4. 关闭中
baseline 持续下降，整体接近单调关闭。

5. 稳定打开-带波动
由打开过程进入稳定状态，baseline 基本稳定，但内部存在局部极值、短暂停顿或有限上下波动。

6. 稳定关闭-带波动
由关闭过程进入稳定状态，baseline 基本稳定，但内部存在局部极值、短暂停顿或有限上下波动。

7. 打开中-带波动
baseline 整体持续上升，内部存在局部下降、极值或暂停，但没有破坏长期打开趋势。

8. 关闭中-带波动
baseline 整体持续下降，内部存在局部上升、极值或暂停，但没有破坏长期关闭趋势。

【波动与独立 segment 的区别】

局部反向运动如果：
- 幅度有限；
- 持续较短；
- 之后回到原 baseline 或恢复原主趋势；
则属于“带波动”，不得单独切段。

如果反向运动：
- 持续明显；
- 相对 GLOBAL 尺度幅度明显；
- 建立新的持续方向；
- 使 baseline 明显迁移；
则必须切成新的 segment。

稳定区域中的多个 MAX/MIN、短平台和短 opening/closing，只要始终围绕同一 baseline，不得拆成多个 segment。

segment 应取最大连续范围，避免过度切分。

【关键帧】

先描述所有候选关键帧，再进行 segment 切分。

每个 keyframe 必须描述：
- before：该点之前的局部轨迹；
- point：该点自身的轨迹特征；
- after：该点之后的局部轨迹。

segment.events 不是该段内所有候选点，而是最终需要保留的结构关键帧。

通常一个 segment 的 events 为：
[开始边界, 结束边界]

相邻 segment 可以共享同一个边界关键帧。

未被任何 segment.events 引用的已有候选关键帧，默认删除。

只有现有候选点无法表达必要结构边界时，才允许 additions。
不得为内部波动补点。

严格输出 JSON，不输出 JSON 之外的内容。
"""


USER_PROMPT = """
请审核当前单夹爪轨迹切片。

你将依次看到：

1. GLOBAL：整个 episode 的全局平滑轨迹；
2. LOCAL：当前局部轨迹，包括目标区域及左右上下文；
3. 当前目标区域内的候选关键帧 metadata。


【GLOBAL】

GLOBAL 用于：

- 判断当前局部变化相对于整个 episode 的幅度大小；
- 判断当前目标区域是否属于更长的稳定、打开或关闭结构；
- 粗略判断当前目标区域大约包含几个连续状态区间及其先后顺序。

不要对整个 episode 做完整分段。

GLOBAL 中：

- 黄色区域：当前目标区域；
- 浅蓝区域：LOCAL 中提供的左右上下文；
- 轨迹为平滑轨迹。


【GLOBAL 粗分段】

首先根据 GLOBAL 粗略判断黄色目标区域大约包含几个连续状态区间。

粗分段只判断三种基础状态：

- 稳定：baseline 基本不变；
- 打开：baseline 整体持续上升；
- 关闭：baseline 整体持续下降。

粗分段只提供：

- 大致有几个区间；
- 各区间的大致顺序；
- 每个区间的宏观特点。

不要依赖 GLOBAL 精确确定边界，也不要因为局部小波动增加粗分段。

输出为：

"coarse_segments": [
  {
    "id": "001",
    "state": "稳定",
    "description": "目标区域前部整体较平缓。"
  },
  {
    "id": "002",
    "state": "关闭",
    "description": "随后出现持续下降。"
  }
]

GLOBAL 粗分段只是先验。

后续必须使用 LOCAL 和左右上下文重新检查。
如果 LOCAL 提供了明确证据，可以合并、拆分或修正 coarse_segments。


【LOCAL】

LOCAL 用于精确判断当前区域。

黄色区域是当前目标区域。
黄色区域之外是左右相邻上下文。

请完整观察左右上下文，不要只观察紧邻边界的少量点。

如果不存在左侧上下文：

    adjacent_regions.left = null

如果不存在右侧上下文：

    adjacent_regions.right = null


【分析要求】

第一步：描述左右相邻区域。

left/right 的 description 描述对应相邻区域自身的整体轨迹特点，包括：

- 整体稳定还是持续变化；
- baseline 上升、下降还是基本稳定；
- 是否存在明显波动；
- 是否包含多个明显阶段。

不要只描述与目标区域连接处的瞬时变化。


第二步：描述当前目标区域。

region.description 概括当前黄色区域的整体轨迹特点和主要变化过程。
不要逐点描述。


第三步：逐个分析所有候选关键帧。

每个候选关键帧输出：

{
  "id": "009",
  "before": "...",
  "point": "...",
  "after": "...",
  "action": {
    "motion": "...",
    "magnitude": "...",
    "duration": "..."
  },
  "relation": {
    "previous": "...",
    "next": "..."
  }
}

字段含义：

before：
描述当前关键帧之前一小段连续轨迹的状态。

point：
描述当前关键帧自身的局部轨迹特点，例如：
局部极值、平台边界、趋势变化位置，
或者稳定区域内部的小幅波动点。

after：
描述当前关键帧之后一小段连续轨迹的状态。


action：
综合 before → point → after，
概括当前关键帧附近对应的局部运动。

motion 可使用：

- 打开
- 关闭
- 稳定
- 打开后关闭
- 关闭后打开
- 打开后稳定
- 关闭后稳定
- 稳定后打开
- 稳定后关闭
- 局部波动

magnitude 表示该运动相对于 GLOBAL 整体开合尺度的变化幅度：

- 小幅
- 中等幅度
- 较大幅度

duration 表示该局部运动在时间轴上的持续程度：

- 短时
- 中等时长
- 长时

不要因为 LOCAL 图被放大而高估 magnitude。


relation：
描述当前关键帧与前后候选关键帧之间整段轨迹的关系。

previous：
当前点与前一个候选关键帧之间主要是什么连续轨迹状态，
以及当前点相对于前一关键帧是否形成结构变化。

next：
当前点与后一个候选关键帧之间主要是什么连续轨迹状态，
以及当前点相对于后一关键帧是否形成结构变化。

不要只比较两个关键帧的数值大小，
要观察两个关键帧之间的完整轨迹。

如果不存在前一个或后一个候选关键帧，对应字段为 null。


id 必须直接使用输入中的三位数字编号，例如：

"001"
"009"
"016"

不要添加 E、P、N 等前缀。


第四步：进行 segment 切分。

segment 表示一种宏观轨迹状态持续成立的最大连续时间区间。

先判断三种基础状态：

1. 稳定：
   baseline 在一段持续时间内基本不变。

2. 打开：
   baseline 持续上升。

3. 关闭：
   baseline 持续下降。


正式切分时：

1. 参考 GLOBAL 的 coarse_segments；
2. 使用 LOCAL 精确观察轨迹；
3. 使用左右上下文判断当前状态是否向目标区域之外连续；
4. 修正 coarse_segments 后得到最终 segments。


只有基础状态发生持续、明确的变化时，才建立新的 segment。

例如：

稳定 → 打开
稳定 → 关闭
打开 → 稳定
关闭 → 稳定
打开 → 关闭
关闭 → 打开

如果前后都形成了独立、持续的状态，就必须切段。


不得只根据整个目标区域起点和终点的净变化进行分类。

例如：

长时间平缓
→ 随后持续下降

必须切成：

稳定类
+
关闭中类

不能因为整个目标区域最终下降，
就全部归为“关闭中”。


短时、小幅、随后恢复原 baseline 或原主趋势的变化，
属于当前 segment 内部波动，不单独切段。

例如：

- 短时反向运动；
- 小幅局部极值；
- 短暂停顿；
- 很短的近似平缓区间。

如果某个稳定、打开或关闭过程本身持续明显，
建立了独立 baseline 或持续运动方向，
则必须成为独立 segment。


“最大连续 segment”是指：

在基础状态不变的前提下尽量保持连续。

不能为了减少 segment 数量，
跨越真实的稳定 / 打开 / 关闭状态切换。


基础状态确定后，再判断是否“带波动”，并映射为以下 8 种最终类型：

1. 稳定打开
   打开运动结束后形成稳定状态，baseline 基本稳定，无明显内部波动。

2. 稳定关闭
   关闭运动结束后形成稳定状态，baseline 基本稳定，无明显内部波动。

3. 打开中
   baseline 持续上升，整体接近连续打开。

4. 关闭中
   baseline 持续下降，整体接近连续关闭。

5. 稳定打开-带波动
   由打开过程进入稳定状态，baseline 基本稳定，
   内部存在局部极值、短暂停顿或有限上下波动。

6. 稳定关闭-带波动
   由关闭过程进入稳定状态，baseline 基本稳定，
   内部存在局部极值、短暂停顿或有限上下波动。

7. 打开中-带波动
   baseline 整体持续上升，
   内部存在局部下降、极值或短暂停顿，
   但没有破坏长期打开趋势。

8. 关闭中-带波动
   baseline 整体持续下降，
   内部存在局部上升、极值或短暂停顿，
   但没有破坏长期关闭趋势。


稳定打开 / 稳定关闭表示进入该稳定状态之前的主要运动方向，
不表示夹爪物理上完全打开或完全关闭。


【波动与新区间】

判断重点是宏观状态是否真正改变，而不是局部斜率是否改变。

如果局部变化：

- 幅度有限；
- 持续较短；
- 随后回到原 baseline；
  或重新恢复原来的打开/关闭主趋势；

则属于“带波动”。

如果局部变化：

- 持续明显；
- 相对 GLOBAL 尺度幅度明显；
- 建立新的持续运动方向；
- 或形成新的稳定 baseline；

则说明建立了新的宏观状态，
必须切成新的 segment。

稳定区域内部即使存在多个 MAX/MIN、短平台或短开合运动，
只要始终围绕同一 baseline，
仍应保持为一个稳定类 segment。


第五步：确定每个 segment 的 events。

events 只列该 segment 最终需要保留的已有候选关键帧，
按时间顺序排列。

通常：

"events": ["开始边界", "结束边界"]

例如：

{
  "id": "001",
  "type": "稳定关闭-带波动",
  "description": "整体保持低值稳定，内部存在局部极值和短平台，但始终围绕同一 baseline。",
  "events": ["009", "016"]
}

009 和 016 之间即使还有多个候选 MAX/MIN/PL/PR，
只要属于该 segment 内部波动，
就不要写入 events。

相邻 segment 可以共享同一个边界关键帧。

例如：

segment 001:
    events = ["004", "009"]

segment 002:
    events = ["009", "016"]

此时 009 同时表示：

- segment 001 的结束；
- segment 002 的开始。

这是正常的边界复用。


events 不需要机械凑成两个点。

如果 segment 的真实开始或结束位于当前目标区域之外，
不要为了凑齐首尾边界而保留无意义候选点。

没有被任何 segment.events 引用的已有候选关键帧，
默认删除。


【本阶段禁止补点】

本阶段只审核和清洗已有候选关键帧。

不要：

- 新增关键帧；
- 推测缺失关键帧；
- 修改已有关键帧类型；
- 输出 additions。

缺失关键帧将在已有关键帧清洗完成后，
由后续独立阶段重新检查相邻保留关键帧之间的完整轨迹。


【输出格式】

{
  "region": {
    "adjacent_regions": {
      "left": {
        "description": "..."
      },
      "right": {
        "description": "..."
      }
    },

    "description": "...",

    "coarse_segments": [
      {
        "id": "001",
        "state": "稳定",
        "description": "..."
      },
      {
        "id": "002",
        "state": "关闭",
        "description": "..."
      }
    ],

    "keyframes": [
      {
        "id": "009",
        "before": "...",
        "point": "...",
        "after": "...",
        "action": {
          "motion": "关闭后稳定",
          "magnitude": "中等幅度",
          "duration": "中等时长"
        },
        "relation": {
          "previous": "与前一候选关键帧之间整体构成持续关闭过程。",
          "next": "与后一候选关键帧之间整体处于同一稳定区域。"
        }
      }
    ],

    "segments": [
      {
        "id": "001",
        "type": "稳定关闭-带波动",
        "description": "...",
        "events": ["009", "016"]
      }
    ]
  },

  "needs_review": false
}

只输出 JSON。
"""